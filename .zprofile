# Adding the path for homebrew
eval "$(/opt/homebrew/bin/brew shellenv)"

# ZSH DOT Directory
export ZDOTDIR="$HOME/.config/zsh"

# Export editor
export EDITOR="nvim"

# Export TMUX Dir
export TMUX_CONFIG="$HOME/.config/tmux/tmux.conf"
